# Review Dispatch

- Timeline while no return: 10min ask to converge; 15min demand the verdict with settled findings and unchecked scope named; 20min cancel and take partial.
- Re-review a fix through the same idle reviewer via `hub`; send only the fix and unchecked scope, then follow its live transcript for the next verdict.
