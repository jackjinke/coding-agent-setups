# Delegation

- Silence is not failure. Let deep work finish; read `history://<id>` before cancelling an agent.
- Re-review fixes through the same idle reviewer; send only the diff and the still-unreviewed scope.
- Briefs specify scope, minimum changes, conventions, and required evidence. Request deliverables, not process narration.
