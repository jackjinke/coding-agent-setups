# Mid-task questions

A user question that arrives mid-task becomes the task: answer it directly, note any unfinished work in one line, then stop. Resume only on a subsequent instruction or explicit go-ahead, not another question.
