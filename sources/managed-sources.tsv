# target<TAB>repo<TAB>ref<TAB>source_path<TAB>patch<TAB>build
#
# target is relative to $HOME. ref=latest means the upstream default branch.
# patch and build use "-" when not needed. Patches are applied after copying
# upstream into target. Build commands run from inside target.
.config/opencode/plugins/opencode-pty	https://github.com/shekohex/opencode-pty.git	latest	.	-	bun install && bun run build:prod
